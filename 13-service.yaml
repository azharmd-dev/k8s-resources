apiVersion: v1
kind: Service
metadata:
  name: frontend-nginx
spec:
  selector: 
    project: robomart
    component: frontend
    envionment: dev
  ports:
   - protocol: TCP
     port: 80 #service port
     targetPort: 80 #container port
